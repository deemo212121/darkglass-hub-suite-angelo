/**
 * CSR To Do List tab (CSRMainDashboard.tsx) — placeholder for now. Used to
 * be a live per-status ticket breakdown (Left Voicemail/CS Need
 * Scheduling/CSR Acknowledge/OP Waiting counts + overdue thresholds);
 * emptied out at the user's request while the real version is reworked.
 */

export function CSRToDoListContent() {
  return (
    <div className="panel p-10 text-center text-sm text-muted-foreground">
      Working on it — coming soon.
    </div>
  );
}
